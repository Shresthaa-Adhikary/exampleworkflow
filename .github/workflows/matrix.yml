name: matrix
on: push

jobs:
  Node-versions:
    runs-on: ubuntu-latest
    steps:
      - name: node version
        run: node -v
      - uses: actions/setup-node@v1
        with:
          node-version: 8
      - name: after setup node version
        run: node -v